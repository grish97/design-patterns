/**
 * Builder Pattern
 *
 * Builder pattern is classified in the creational design patterns
 * which is all about class/object instantiation.
 *
 * The builder pattern itself is used to separate the construction
 * of a complex object from its representation.
 */

/**
 * The Builder pattern’s main intent is to have the minimum number
 * of overloading constructors to support the constuction of several
 * representations of an object.
 */
